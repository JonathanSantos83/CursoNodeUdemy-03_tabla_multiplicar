{
  "name": "app_multiplicar_consola",
  "version": "0.0.1",
  "description": "Es una simple tabla de multiplicar que genera un archivo de texto con la misma",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Santos",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "yargs": "^16.2.0"
  }
}
